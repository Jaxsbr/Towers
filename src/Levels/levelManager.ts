export class LevelManager {
  constructor() {
    // TODO:
    // Load all levels from a file
    // Expose and set the current level
    // Set milestones for level completed (E.g. enemies killed, reached end)
    // Implement next level function
  }
}
